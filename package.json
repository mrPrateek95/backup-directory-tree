{
  "name": "backup-directory-tree",
  "version": "0.0.0",
  "description": "Backs up the directory tree of an external HDD (with a certain tag) when it is connected to the Mac.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mrprateek95",
  "license": "ISC",
  "dependencies": {
    "directory-tree": "^2.2.4",
    "node-notifier": "^6.0.0",
    "osx-tag": "^0.4.1"
  }
}
